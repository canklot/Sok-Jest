/* import TestComponent from '../components/BannerB.vue'
import { mount } from '@vue/test-utils' */

test('mount a vue component', () => {
    /* const wrapper = mount(TestComponent)
    expect(wrapper).toMatchSnapshot() */
    expect(1).toMatchSnapshot(1)
  })

